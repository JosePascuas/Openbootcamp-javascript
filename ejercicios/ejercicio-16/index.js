let nombre = "Jose Miguel"
let apellido= "Pascuas Carvajal"

const obj= {
  nombre,
  apellido
}

// localStorage.setItem("persona", JSON.stringify(obj))
// sessionStorage.setItem("persona", JSON.stringify(obj))

// document.cookie=`persona=${JSON.stringify(obj)}; expires=` + new Date(Date.now() + 2 * 60 * 1000).toUTCStringS